---
import Prose from "./Prose.astro";

interface ProseProps {
  class?: string;
}

const { class: additionalClass = '' } = Astro.props;

---

<Prose class={`mx-auto ${additionalClass}`}>
    <slot/>
</Prose>
